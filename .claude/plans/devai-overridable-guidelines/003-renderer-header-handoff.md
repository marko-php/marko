# Task 003: AgentsMdRenderer — hand the generated header off to the marker writer

**Status**: completed
**Depends on**: 001
**Retry count**: 0

## Description
Stop the renderer from emitting its own standalone `<!-- Generated by marko/devai. Do not edit manually… -->` header (line 29), since that semantic now lives in the `GuidelinesWriter` BEGIN marker. This prevents a double "do not edit" stamp when the body is wrapped in markers, and keeps the do-not-edit boundary in exactly one place.

## Context
- File: `packages/devai/src/Rendering/AgentsMdRenderer.php` (line 29 emits the header into `$sections`).
- The renderer still produces the full body (project name, guidelines, etc.); only the standalone generated-header line is removed/relocated.
- Coordinate the wording so the writer's BEGIN marker carries the regenerate hint; the body itself should no longer claim "do not edit manually" (since content outside markers IS editable).
- Update renderer tests under `packages/devai/tests/Unit/Rendering/AgentsMdRendererTest.php`. The specific test that breaks is `it('includes a "do not edit" marker with regeneration instructions', ...)` (lines 66–70) which asserts `$result->body` contains `'Generated by marko/devai'` and `'marko devai:update'`. That assertion must be removed or rewritten — after this task the rendered body carries NEITHER string (both now live in the `GuidelinesWriter` BEGIN marker per task 001). Replace it with a test asserting the body does NOT contain `'Generated by marko/devai'`. Do not leave a dangling assertion expecting the regenerate hint in the body.

## Requirements (Test Descriptions)
- [x] `it does not emit a standalone generated-by header in the rendered body`
- [x] `it still renders the project name heading`
- [x] `it still renders the package guidelines section`

## Acceptance Criteria
- All requirements have passing tests
- The do-not-edit semantics exist only in the writer's marker, not duplicated in the body
- Renderer tests updated
- Code follows code standards

## Implementation Notes
- Removed the single `$sections[] = "<!-- Generated by marko/devai... -->\n";` line (line 29) from `AgentsMdRenderer::render()`. The "do-not-edit" semantic now lives exclusively in the `GuidelinesWriter` BEGIN marker (task 001).
- Rewrote the existing `it('includes a "do not edit" marker...')` test to `it('does not emit a standalone generated-by header in the rendered body')` asserting `not->toContain('Generated by marko/devai')`.
- Added two new passing tests: `it still renders the project name heading` and `it still renders the package guidelines section`.
- All 185 devai package tests pass; both touched files are lint-clean.
